version: "3"
services:
  backend-api:
    image: backend-api
    environment:
      DB_USERNAME: $DB_USERNAME
      DB_PASSWORD: $DB_PASSWORD 
      DB_HOST: $DB_HOST 
      DB_PORT: $DB_PORT
      DB_NAME : $DB_NAME